FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir fastapi uvicorn

COPY ./app/services/ai_mock.py ./ai_mock.py

EXPOSE 8001

CMD ["uvicorn", "ai_mock:app", "--host", "0.0.0.0", "--port", "8001"]
